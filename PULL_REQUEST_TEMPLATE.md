# NA

## Summary

What does this PR change? Link related issues with `Fixes #123` or
`Refs #123`.

## Checklist

Source changes were made in the relevant `dev/flat_*.Rmd` file and
inflated with `fusen::inflate()` (not edited directly under `R/`)

`devtools::document()` has been run

`devtools::check()` returns 0 errors / 0 warnings / 0 notes

Tests have been added or updated under `tests/testthat/`

`NEWS.md` has a bullet under the development version describing the
user-visible change (if any)
